import * as PrismaPackage from '@prisma/client';

export import Prisma = PrismaPackage.Prisma;
export const PrismaClient = PrismaPackage.PrismaClient;
export type PrismaClient = PrismaPackage.PrismaClient;
